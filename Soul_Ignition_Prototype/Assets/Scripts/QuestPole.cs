using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class QuestPole : MonoBehaviour
{
   /*
    public NPCFaceChange faceChangeScript;

    //public GameObject charOneFrown;
    //public GameObject charOneSmile;
    public Collider questCollider;
    public KeyCode questOneAction = KeyCode.E;

    public bool inQuestCollider;
    public bool doFaceChange;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    private void OnTriggerEnter(Collider other)
    {
        QuestPoleFunction();
    }

    public void QuestPoleFunction()
    {
        //Debug.Log("Player entered collider");
        inQuestCollider = true;


        if (inQuestCollider) // Check if player entered collider
        {
            Debug.Log("Player entered collider");
            //inQuestCollider = true;

            if (Input.GetKeyDown(questOneAction))
            {
                doFaceChange = true;
                faceChangeScript.ChangeCharFace();
                Debug.Log("ChangeCharFace function called - Part 1");
            }
           
        }
    }

    private void OnTriggerExit(Collider other)
    {
        inQuestCollider = false;
    }
   */
}
